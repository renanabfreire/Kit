#include "BnC.h"

std::vector<int> Max_Back(Data& data,std::vector<int> so, Solution& sol){
    std::vector<int> maxback_val(data.getDimension(), 0);
    for(int i=0; i<so.size(); i++){
        
    }
}